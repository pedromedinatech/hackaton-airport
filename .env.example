# Copy to .env.local and fill in. .env.local is gitignored — never commit real keys.

# Anthropic API key for the AI Assistant chatbot (/api/chat → Claude Haiku).
# Server-side only. Do NOT prefix with NEXT_PUBLIC_ (that would leak it to the browser).
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
